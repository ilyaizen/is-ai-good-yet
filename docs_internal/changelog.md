# Changelog notes

The canonical project changelog is `../CHANGELOG.md`.

It was restored from the legacy/private project history and normalized into the public `ilyaizen/is-ai-good-yet` repo. Keep release history in the root changelog; use this file only as the internal-docs index pointer so agents find it while reading `docs_internal/`.
