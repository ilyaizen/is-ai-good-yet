# CLI & UX
rich

# Data Processing
polars
pyarrow  # Required for Polars parquet support

# Network & Scraping
aiohttp
aiolimiter
tenacity
trafilatura
playwright
playwright-stealth-plus
curl-cffi  # Optional: TLS fingerprint randomization
newspaper3k
lxml-html-clean  # newspaper3k compatibility with modern lxml
setuptools<81  # playwright-stealth-plus still imports pkg_resources
importlib-metadata

# Selenium + undetected-chromedriver (Cloudflare bypass for archive.is)
undetected-chromedriver
selenium
seleniumbase

# LLM & Validation
ollama
pydantic
groq

# Environment
python-dotenv

# Testing
pytest
pytest-asyncio

# Linting
sloppylint[rich]  # AI slop detector (does NOT include black/ruff — install separately if needed)
